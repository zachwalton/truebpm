{
  "engines": {
    "node": "16.14.2"
  },
  "scripts": {
    "postinstall": "cd frontend && yarn install --only=dev && yarn install && yarn build"
  },
  "dependencies": {
    "yarn": "^1.22.18"
  }
}
